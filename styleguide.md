# Logging
Before start of every function their should a Log statement to note that function has started. The log should contain value of parameters passed to it.

For example,
int add(int a, int b) {
    System.out.println("Adding 2 numbers started");
    return a+b;
}
